# Project AGENTS.md

## 项目身份

- 项目名称：ComfyNeko。
- 分类：软件开发。
- 根目录：`H:\AIGC\Codex\Projects\软件开发\ComfyNeko`。
- 目标：以生成记录（Run）为中心，关联 ComfyUI 环境、模型、输入、输出、提示词和工作流。
- 品牌语：守住灵感，找回每一次生成。
- 当前阶段：设计完成，优先开发环境绑定向导；详见 `docs/`。

## 范围与边界

- 首版是 Windows 本地优先桌面应用，支持多环境档案；ComfyUI Connector 是可选适配器。
- 默认只读。移动、实体重命名、删除、工作流修复、插件/环境维护都必须先生成变更计划并允许撤销。
- 不把真实模型文件、输入素材或 output 文件复制进项目数据库；仅保存索引、缩略图、元数据、哈希和关系。
- 不修改用户的 ComfyUI、Python、`extra_model_paths.yaml`、`custom_nodes` 或启动配置，除非操作有明确确认、备份和验证路径。
- 不混入其他 ComfyUI 工具、已有安装包或反编译代码。可参考行为与公开许可代码，代码复用须单独确认许可。

## 建议技术基线

- 桌面端：Tauri 2 + React + TypeScript。
- 本地核心：Rust，SQLite（WAL + FTS5），缩略图与任务队列由本地核心管理。
- ComfyUI 连接器：独立、最小化 Python custom node；只暴露经过身份校验的本地接口。
- 目标平台：Windows 10/11；所有路径逻辑需使用 Windows `PathBuf`，不可假设 POSIX 路径。

## 工作规则

- 修改前先读本文件、README 和对应 `docs/` 设计文档。
- 修改功能前还必须遵循 `docs/07-开发执行与体验基线.md`：测试先行、隔离分支、可验证子里程碑日志/推送与 Desktop UI 质量门槛均为强制要求。
- 先为新增行为写最窄测试，再实现；完成后运行对应单测、格式化、类型检查与构建。
- 所有文件系统操作必须通过路径白名单与规范化路径检查；删除使用应用回收站或系统回收站。
- 任何来自 workflow JSON、图片 metadata、同名 `.txt`、网络 API 的字段都标记来源和可信度，不能静默覆盖用户编辑。
- 产物放在 `outputs/`；过程资料放在 `work/`。每个可验证子里程碑必须更新日志、路线图和 README 后提交并推送；`main` 只接收已通过该里程碑验收的版本。

## 验证基线

- 环境绑定：验证根目录、解释器、目录访问、配置解析和可选 API 连通性。
- 索引：必须覆盖重复扫描、移动文件、失效路径、不可读文件和取消任务。
- 生成复现：图片、视频、音频或 sidecar manifest 任一来源都要显示数据来源；不可恢复时要说明缺失项。
- 变更：每个写操作需要预览、日志、撤销或明确说明为何不可撤销。
